# ===========================================
# MeetMind Configuration
# ===========================================

# Provider selection: openai | local | huggingface
EMBEDDINGS_PROVIDER=local
LLM_PROVIDER=local

# ===========================================
# OpenAI Configuration (if using openai provider)
# ===========================================
OPENAI_API_KEY=sk-your-openai-key-here

# ===========================================
# HuggingFace Configuration (if using huggingface provider)
# ===========================================
HUGGINGFACE_API_TOKEN=hf_your-huggingface-token-here

# ===========================================
# Local Model Configuration
# ===========================================
# Embedding model (for local provider)
LOCAL_EMBEDDING_MODEL=BAAI/bge-m3

# Ollama model (for local LLM provider)
OLLAMA_MODEL=llama3
OLLAMA_BASE_URL=http://localhost:11434

# HuggingFace model IDs (for huggingface provider)
HF_EMBEDDING_MODEL=BAAI/bge-m3
HF_LLM_MODEL=mistralai/Mistral-7B-Instruct-v0.3

# ===========================================
# Database Configuration
# ===========================================
DATABASE_URL=sqlite+aiosqlite:///./meetmind.db

# ===========================================
# ChromaDB Configuration
# ===========================================
CHROMA_PERSIST_DIRECTORY=./chroma_db

# ===========================================
# RAG Configuration
# ===========================================
# RAG prompt template (must include {context} and {question} placeholders)
RAG_PROMPT_TEMPLATE="Ты ассистент для анализа рабочих созвонов.
Отвечай только на основе предоставленного контекста.
Если информации нет в контексте, скажи об этом честно.

Контекст:
{context}

Вопрос: {question}

Ответ:"

# ===========================================
# WhisperX Configuration (Audio Transcription)
# ===========================================
# Model size: tiny, base, small, medium, large-v2, large-v3
WHISPERX_MODEL=large-v3
# Device: cuda (GPU) or cpu
WHISPERX_DEVICE=cuda
# Compute type: float16, int8 (GPU), float32 (CPU)
WHISPERX_COMPUTE_TYPE=float16
# Directory for temporary media uploads
MEDIA_UPLOAD_DIR=./media_uploads
